language: python

cache:
  pip: true
  directories:
    - $HOME/.cache/pip

env:
  global:
    - DIFF_RESULTS_BUCKET=somewhere...

install:
  - git clone https://github.com/NyanKiyoshi/pytest-django-queries-ci-tools.git --depth 1 ../queries-ci-tools
  - pip install --pre pytest-django-queries

script:
  - ../queries-ci-tools/handle-event.sh
